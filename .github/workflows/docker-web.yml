name: Build and Deploy NOC-Agent Web

on:
  push:
    branches: [ main ]
    paths:
      - 'web/**'
      - 'docker-compose.web.yml'
      - '.github/workflows/docker-web.yml'
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repositório
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login no GitHub Container Registry (GHCR)
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and Push Imagem Web
        uses: docker/build-push-action@v6
        with:
          context: ./web
          file: ./web/Dockerfile
          push: true
          tags: ghcr.io/wittemberg/nocagent-web:latest

      - name: Notificar Portainer via Webhook da Stack Web
        env:
          WEB_WEBHOOK: ${{ secrets.PORTAINER_WEB_WEBHOOK_URL }}
          LEGACY_STACK_WEBHOOK: ${{ secrets.PORTAINER_STACK_WEBHOOK_URL }}
        run: |
          WEBHOOK_URL="${WEB_WEBHOOK:-$LEGACY_STACK_WEBHOOK}"
          if [ -n "$WEBHOOK_URL" ]; then
            echo "🚀 Disparando webhook do Portainer..."
            RESPONSE=$(curl -sk -w "\nHTTP_STATUS:%{http_code}" -X POST "$WEBHOOK_URL")
            echo "$RESPONSE"
            HTTP_STATUS=$(echo "$RESPONSE" | grep "HTTP_STATUS:" | cut -d':' -f2)
            if [ "$HTTP_STATUS" -ge 200 ] && [ "$HTTP_STATUS" -lt 300 ]; then
              echo "✅ Webhook aceito pelo Portainer com sucesso (Status: $HTTP_STATUS)!"
            else
              echo "⚠️ Portainer retornou código HTTP $HTTP_STATUS ao processar o webhook."
            fi
          else
            echo "⚠️ Nem PORTAINER_STACK_WEBHOOK_URL nem PORTAINER_WEB_WEBHOOK_URL foram configurados nos secrets."
          fi
